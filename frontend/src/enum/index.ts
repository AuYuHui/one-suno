export enum SetupStoreId {
  App = 'app-store',
  Theme = 'theme-store',
  Auth = 'auth-store',
  Route = 'route-store',
  Tab = 'tab-store'
}

// Suno 账号类型 是否可用枚举
export enum AccountType {
  /** google */
  GOOGLE = 'google',
  /** microsoft */
  MICROSOFT = 'microsoft'
}
